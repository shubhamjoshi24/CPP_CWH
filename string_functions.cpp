#include <bits/stdc++.h>
using namespace std;
int main()
{

    char s1[] = "shubham ";
    char s2[] = " joshi";
    char s3[30] = " sonu ";

    // puts(strcat(s1, s2));
    // cout << "s1 length " << strlen(s1) << endl;
    // cout << "s2 length " << strlen(s2) << endl;

    // cout << "the reverse of s1 - ";
    // puts(strrev(s1));
    // cout << "the reverse of s2 - ";
    // puts(strrev(s2));

    // strcpy(s3, strcat(s1, s2));
    // puts(s3);

    cout << strcmp(s1, s2) << endl;

    return 0;
}