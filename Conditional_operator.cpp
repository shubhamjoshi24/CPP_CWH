#include <bits/stdc++.h>
using namespace std;
int main()
{
    // Conditional Operator or Ternary Operator
    int a, b;
    cin >> a;
    b = ((a == 5) ? (3) : (90));
    cout << a << endl;
    cout << b << endl;
    return 0;
}